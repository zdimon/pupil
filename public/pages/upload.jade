h1 upload
form
    input(type="file" nv-file-select uploader="uploader")

    ul
        li(ng-repeat="item in uploader.queue") Name:
            span(ng-bind="item.file.name")
            button(ng-click="item.upload()") upload


    button(ng-click="upload()" class="btn btn-success") Go
